# 画像保存ツール 操作方法

## 1. 初回セットアップ

PowerShell でプロジェクトのフォルダへ移動します。

```powershell
cd C:\work\karinfuji
```

仮想環境を作成し、必要なパッケージをインストールします。

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e .
.\.venv\Scripts\python.exe -m playwright install chromium
```

このセットアップは通常一度だけ必要です。

## 2. 専用 Edge を起動する

通常の Edge を起動するコマンドではなく、ツールから接続できる専用 Edge を起動します。

```powershell
& "${env:ProgramFiles(x86)}\Microsoft\Edge\Application\msedge.exe" --remote-debugging-port=9222 --user-data-dir="$PWD\.data\edge-profile"
```

Edge のインストール場所によって起動できない場合は、次を試してください。

```powershell
& "$env:ProgramFiles\Microsoft\Edge\Application\msedge.exe" --remote-debugging-port=9222 --user-data-dir="$PWD\.data\edge-profile"
```

このコマンドを実行すると、専用プロフィールの Edge が新しく開きます。

`--remote-debugging-port=9222` は、画像保存ツールが Edge の通信を監視するために必要です。

`--user-data-dir` は、通常の Edge のプロフィールとは別に、ツール専用のプロフィールを使う指定です。

## 3. Edge で手動ログインする

専用 Edge が開いたら、次の操作を手動で行います。

1. 対象サイトを開く
2. ユーザー名やパスワードを入力してログインする
3. 画像一覧の画面まで移動する
4. 画像一覧が表示されている状態にする

パスワードはツールへ入力しません。また、ツールにも保存しません。

## 4. 画像通信の監視を開始する

Edge は開いたままにして、別の PowerShell を開きます。

```powershell
cd C:\work\karinfuji
.\.venv\Scripts\python.exe -m app.phase1 `
  --cdp-url "http://127.0.0.1:9222" `
  --output-dir output
```

次のように対象タブの URL が表示されたら監視中です。

```text
画像通信を監視中です。対象タブ: https://example.com/image-list
終了するには Ctrl+C を押してください。
```

特定のページを監視したい場合は、URL の一部を指定できます。例えば、対象ページの URL に `message-list` が含まれる場合は次のように実行します。

```powershell
.\.venv\Scripts\python.exe -m app.phase1 --cdp-url "http://127.0.0.1:9222" --page-url-contains "message-list" --output-dir output
```

URL に一致するタブがない場合は、接続中のタブ URL の一覧が表示されます。

## 5. 画像を保存する

監視を開始した後、Edge で次の操作を行います。

- 画像一覧を開き直す
- 過去の画像を表示するために上方向へスクロールする
- 必要に応じて一覧をスクロールする

Network に発生する JPEG レスポンス（`Content-Type: image/jpeg`）だけが自動的に `output` フォルダへ `.jpg` として保存されます。PNG、SVG、favicon などは保存しません。

画像 URL をコピーして別タブで開いたり、右クリックしたりする操作は不要です。

### サーバーへの通信について

現在の監視モードでは、ツール自身がサイトを開いたり、クリックしたり、スクロールしたり、画像 URL を再取得したりしません。

処理の流れは次のとおりです。

1. ユーザーが Edge で操作する
2. Edge が通常どおりサイトへリクエストを送る
3. ツールが Edge に届いたレスポンスを検出する
4. Edge が受信済みの画像データをツールがローカルへ保存する

画像データの保存時に、同じ URL へ追加の HTTP リクエストは送信しません。監視中のツールは約0.25秒ごとに Edge から届いたイベントを処理していますが、サイトへ新しいリクエストを送る処理は行いません。

監視中に Edge の読み込みを止めないため、監視プログラムは Playwright のイベント処理を継続的に実行します。画像レスポンスを検出した後、受信済みのレスポンス本体をローカルへ保存します。

ただし、Edge での再読み込みや短時間の大量スクロールは、通常のブラウザ操作としてサイトへの通信を発生させます。DOS と判定されないためにも、必要な範囲でゆっくり操作し、連続した再読み込みや自動化された大量取得は行わないでください。

## 6. 保存を終了する

監視を実行している PowerShell で `Ctrl+C` を押します。

```text
監視を終了しました。保存件数: 10
```

Edge は必要に応じて手動で閉じてください。

## 7. 対象外の画像が保存される場合

サイト内のアイコンなど、対象サイトの別画像まで保存される場合があります。

画像 URL に共通する文字列が分かっている場合は、`--url-contains` を指定します。JPEG 限定の判定は常に有効です。

```powershell
.\.venv\Scripts\python.exe -m app.phase1 `
  --cdp-url "http://127.0.0.1:9222" `
  --output-dir output `
  --url-contains ".jpg"
```

実際の URL に共通する文字列に合わせて `.jpg` の部分を変更してください。

## 8. よくある問題

### Edge を起動できない

Edge の場所を確認してください。

```powershell
Test-Path "${env:ProgramFiles(x86)}\Microsoft\Edge\Application\msedge.exe"
Test-Path "$env:ProgramFiles\Microsoft\Edge\Application\msedge.exe"
```

どちらかが `True` になるパスを起動コマンドで使います。

### 接続できない

次を確認してください。

- 専用 Edge が起動しているか
- 起動コマンドに `--remote-debugging-port=9222` があるか
- ツール実行前に Edge を終了していないか
- ツールと Edge が同じ PC 上で動いているか

### 画像が保存されない

次を確認してください。

- 監視開始後に画像一覧を開いたか
- 監視開始後に対象画像が読み込まれたか
- 画像一覧をスクロールしたか
- 対象画像が `image/*` のレスポンスとして返っているか
- `--url-contains` を指定している場合、その文字列が URL に含まれているか

監視開始前に読み込まれた通信は取得できません。必ず監視開始後に一覧を開くか、スクロールしてください。

## 9. 実行の流れ（短縮版）

```text
1. 専用 Edge を起動
2. Edge でログイン
3. 対象の画像一覧を開く
4. 別の PowerShell で監視コマンドを実行
5. Edge で一覧を開く、またはスクロールする
6. output フォルダを確認する
7. Ctrl+C で監視を終了する
```
